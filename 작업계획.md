# CommuteFit 리액트 마이그레이션 작업 계획서

## 1. 프로젝트 개요
- **목표:** 기존 순수 HTML/CSS/JS로 퍼블리싱된 CommuteFit 웹 리소스를 React(Vite) + TypeScript 환경으로 마이그레이션
- **원본 소스 위치:** `D:\조진훈\CommuteFit\sta\`
- **프레임워크:** React (Vite)
- **언어:** TypeScript
- **스타일링:** styled-components
- **라우팅:** react-router-dom

---

## 2. 디렉토리 및 아키텍처 설계

### 2.1. 디렉토리 구조
```text
D:\조진훈\CommuteFit\
├── src/
│   ├── assets/              # 이미지, 아이콘 등 정적 파일
│   ├── components/          # 재사용 가능한 UI 컴포넌트
│   │   ├── common/          # 공통 컴포넌트 (Header, Footer 등)
│   │   └── home/            # 메인 페이지 전용 컴포넌트
│   ├── hooks/               # 커스텀 훅 (API 호출, 상태 관리)
│   ├── pages/               # 라우터 기반 페이지 컴포넌트
│   ├── styles/              # GlobalStyles, Theme 설정
│   ├── types/               # TypeScript 인터페이스 및 타입 정의
│   ├── App.tsx              # 최상위 컴포넌트 (라우터 설정)
│   └── main.tsx             # React 엔트리 포인트
├── index.html               # Vite 템플릿
├── vite.config.ts
├── tsconfig.json
└── package.json
```

### 2.2. 컴포넌트 분리 계획 (`src/components/`)
요구사항에 따라 헤더와 푸터를 공통 컴포넌트로 분리하고, 메인 페이지의 각 섹션을 개별 컴포넌트화합니다.

- **공통 컴포넌트 (`src/components/common/`)**
  - `Header.tsx`: 앱 상단 로고 및 설정 버튼 포함 (`.app-header`)
  - `Footer.tsx`: 앱 하단 푸터 (현재 퍼블리싱에는 없으나 요구사항에 맞춰 빈 템플릿이라도 생성 후 추후 확장)

- **메인 페이지 컴포넌트 (`src/components/home/`)**
  - `AirHero.tsx`: 1순위 미세먼지 상태 및 추천 문구 영역 (`.air-hero`)
  - `CostCompare.tsx`: 2순위 이동수단별 비용 비교 영역 (`.cost-area`)
  - `SavingsArea.tsx`: 3순위 절약 금액 및 세부 지표 영역 (`.savings-area`)
  - `TipsArea.tsx`: 4순위 건강 수칙 리스트 영역 (`.tips-area`)
  - `SettingsSheet.tsx`: 출퇴근 정보 설정 바텀 시트 오버레이 (`.settings-overlay`)

### 2.3. 페이지 구성 (`src/pages/`)
- `Home.tsx`: 위에서 분리한 Home 관련 컴포넌트들을 조립하는 메인 컨테이너 컴포넌트. 라우터 경로 `/`에 매핑.

---

## 3. 상태 관리 및 비즈니스 로직 (Hooks)

기존 `script.js`에 존재하는 기능들을 React 훅으로 분리하여 관리합니다.

1. **`useCommuteData.ts` (출퇴근 정보 및 비용 계산 상태 관리)**
   - 로컬 스토리지(`localStorage`) 기반 상태 유지
   - 설정 모달 폼 상태(출발지, 도착지, 거리, 연비, 연료 종류 등) 관리
   - 대중교통 및 자가용 이용 비용, 절감액, 탄소 절감량 계산 로직 포함
2. **`useAirQuality.ts` (미세먼지 API 상태 관리)**
   - 에어코리아 API(getUlfptcaAlarmInfo) 호출 로직
   - 비동기 데이터 패칭 및 상태 관리 (Loading, Error, Data)
   - 결과에 따른 등급 산정 및 팁(Tip) 변경 로직 내장
3. **`useSettingsSheet.ts` (바텀 시트 UI 상태 관리)**
   - 설정창의 Open/Close 상태 제어

---

## 4. 스타일 가이드 (styled-components)

- 기존 `style.css`를 기반으로 `styled-components` 문법으로 변환.
- `src/styles/GlobalStyle.ts`를 생성하여 Reset CSS, 폰트(Pretendard 등), 전역 변수(Color, Size) 세팅.
- 각 컴포넌트 단위로 스타일 분리 작성 (`Header.styles.ts` 파일 분리 또는 컴포넌트 내부에 작성).

---

## 5. 단계별 작업 절차

### 1단계: 프로젝트 초기화
- Vite + React + TS 환경 보일러플레이트 구축
- 필수 패키지 설치: `styled-components`, `react-router-dom`, `@types/styled-components`
- 초기 폴더 구조 생성

### 2단계: 글로벌 스타일 및 공통 컴포넌트 구현
- 원본 `style.css`에서 `:root` 전역 변수, `body` 설정 등을 `GlobalStyle.ts`로 이관
- `Header.tsx`, `Footer.tsx` 개발
- App.tsx에 라우터 세팅 (`/` 경로에 빈 Home 페이지 연결)

### 3단계: 메인 페이지 UI 컴포넌트 마이그레이션 (정적)
- 원본 `index.html` 구조를 React 컴포넌트로 변환
- `AirHero`, `CostCompare`, `SavingsArea`, `TipsArea`, `SettingsSheet` UI를 하드코딩 상태로 구현
- 각 컴포넌트에 해당되는 CSS를 styled-components로 변환하여 적용

### 4단계: 비즈니스 로직 이관 및 상태 연동
- 커스텀 훅(`useCommuteData`, `useAirQuality`) 작성
- `Home.tsx` 컨테이너에서 상태를 생성하고 하위 컴포넌트에 Props로 전달
- `SettingsSheet.tsx`에서 입력값을 받아 비용을 다시 계산하고 LocalStorage에 저장하는 기능 구현
- 에어코리아 API 연동 후 미세먼지 등급별 UI 분기 처리

### 5단계: 최종 점검 및 디버깅
- 데이터 초기 상태(로컬 스토리지 비어있을 때) 대응
- API 호출 실패 시 에러 처리
- 모바일 뷰 최적화 확인 및 마무리
